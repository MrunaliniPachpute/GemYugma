<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lost & Found</title>

  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #5f72ff, #9b23ea);
      margin: 0;
      padding: 60px 0 0 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
    }

    .container {
      text-align: center;
      background: rgba(255, 255, 255, 0.95);
      padding: 55px 50px 60px;
      border-radius: 26px;
      max-width: 650px;
      width: 90%;
      box-shadow: 0 30px 70px rgba(0, 0, 0, 0.18);
      position: relative;
    }

    /* IMAGE LOGO */
    .logo {
      position: absolute;
      top: -45px;
      left: 50%;
      transform: translateX(-50%);
      width: 90px;
      height: 90px;
      border-radius: 50%;
      object-fit: contain;
      background: white;
      padding: 10px;
      box-shadow: 0 15px 35px rgba(0,0,0,0.3);
    }

    h1 {
      margin-top: 40px;
      color: #2b2b2b;
      margin-bottom: 40px;
      font-size: 3em;
      letter-spacing: 1px;
      text-shadow: 0 4px 10px rgba(0,0,0,0.12);
    }

    a {
      text-decoration: none;
    }

    button {
      background: linear-gradient(135deg, #FF6B6B, #4ECDC4);
      border: none;
      color: white;
      padding: 16px 38px;
      margin: 14px;
      border-radius: 60px;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.35s ease;
      box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
      min-width: 280px;
      position: relative;
      overflow: hidden;
    }

    button::after {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(
        120deg,
        transparent,
        rgba(255, 255, 255, 0.4),
        transparent
      );
      transform: translateX(-120%);
      transition: transform 0.6s ease;
    }

    button:hover::after {
      transform: translateX(120%);
    }

    button:hover {
      transform: translateY(-5px) scale(1.04);
      box-shadow: 0 18px 45px rgba(0, 0, 0, 0.35);
    }

    button:active {
      transform: translateY(-2px);
    }

    @media (max-width: 520px) {
      h1 {
        font-size: 2.4em;
      }

      button {
        min-width: 100%;
        font-size: 16px;
        padding: 14px 24px;
      }
    }
  </style>
</head>

<body>
  <div class="container">

    <!-- LOGO IMAGE -->
    <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcT71KAKl0Tvwd5P2SFs1GjveEaaLIwNe-K0Vw&s" alt="Lost & Found Logo" class="logo">

    <h1>GEMYUGMA</h1>

    <a href="/items/new">
      <button>Report Lost / Found Item</button>
    </a>

    <a href="/items">
      <button>View Items</button>
    </a>

    <% if (recentItems && recentItems.length > 0) { %>
    <div style="margin-top: 40px; text-align: left; max-width: 600px; margin-left: auto; margin-right: auto;">
      <h2 style="color: #2b2b2b; font-size: 1.8em; margin-bottom: 20px;">Recent Items</h2>
      <% recentItems.forEach(item => { %>
      <div style="background: rgba(255, 255, 255, 0.9); padding: 15px; border-radius: 10px; margin-bottom: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.1); display: flex; align-items: center;">
        <% if (item.imageUrl) { %>
        <img src="<%= item.imageUrl %>" alt="<%= item.title %>" style="width: 80px; height: 80px; object-fit: cover; border-radius: 8px; margin-right: 15px;">
        <% } %>
        <div>
          <h3 style="margin: 0; color: #2b2b2b; font-size: 1.2em;"><%= item.title %></h3>
          <p style="margin: 5px 0; color: #666;"><%= item.description %></p>
          <small style="color: #999;">Type: <%= item.type %> | <%= item.lostFoundAt ? (item.type === 'lost' ? 'Lost' : 'Found') + ' on: ' + new Date(item.lostFoundAt).toLocaleString() : 'Reported on: ' + new Date(item.createdAt).toLocaleString() %> | Contact: <%= item.contactType %></small>
        </div>
      </div>
      <% }); %>
    </div>
    <% } %>

  </div>
</body>
</html>
